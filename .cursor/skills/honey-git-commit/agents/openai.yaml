interface:
  display_name: "Honey Git Commit"
  short_description: "Summarize staged changes and commit this repo"
  default_prompt: "Use $honey-git-commit to summarize the staged changes in this repo, generate the final git subject, and create the commit."
